<?xml version="1.0" encoding="utf-8"?>
<!-- No compatibility-mode browsers: that mode hands the keyboard to the
     suggestion window, so nothing typed reaches the page. Firefox works on
     its own; Chrome after "Autofill using another service" in its settings. -->
<autofill-service xmlns:android="http://schemas.android.com/apk/res/android" />
